# Ignore all project directories (they have their own git repos)
projects/*/

# Ignore sandbox experiments
sandbox/

# Ignore platform-specific configs (we track .claude/settings.local.json with the configured permissions)
.cursor/
.windsurf/

# Keep the core prompts tracked
!core/

# Obsidian integration - ignore config with personal vault path
obsidian-integration/config.sh

# System files
.DS_Store
*.log
